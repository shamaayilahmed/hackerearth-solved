#include <stdio.h>
#include <string.h>
int main()
{
	char s[20];
    int cz=0,co=0,i;      
	scanf("%[^\n]%*c",s);
	for (int i=0;i<strlen(s);i++){
		if(s[i]=='z'){
			cz=cz+1;
		}
		else{
		 co=co+1;
		}
	}
	if((2*cz)==co){
		printf("Yes");
	}
	else{
		printf("No");
	}
}
